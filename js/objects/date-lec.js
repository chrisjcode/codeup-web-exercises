// Create Date

let today = new Date();

let codeUpDate = new Date();

// DateConstructor.(year: number,
// monthIndex: number,
// date ? : number,
// hours ? : number,
// minutes ? : number, seconds ? : number,
// ms ? : number
// )

codeUpDate = new Date(l, l);